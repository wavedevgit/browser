name: librewolf-custom-build

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version"
        required: false
        default: "dev-build"

jobs:
  linux:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: |
          sudo apt update
          sudo apt install -y \
            wget tar python3 python3-pip \
            rustc cargo clang llvm make \
            nodejs git patch

      - name: Download source
        run: |
          wget -qO source.tar.gz \
          https://codeberg.org/api/packages/librewolf/generic/librewolf-source/149.0.2-2/librewolf-149.0.2-2.source.tar.gz

      - name: Extract
        run: tar xf source.tar.gz

      - name: Detect root
        run: |
          ROOT=$(find . -type f -path "*/dom/base/Navigator.cpp" | head -n 1 | sed 's|/dom/base/Navigator.cpp||')
          echo "ROOT=$ROOT" >> $GITHUB_ENV

      - name: Apply patch
        run: |
          cd "$ROOT"
          patch -p1 < ../webdriver-disable.patch

      - name: Build (low memory)
        run: |
          cd "$ROOT"

          export MOZ_PARALLEL_BUILD=2
          export MOZ_MAKE_FLAGS="-j2"

          echo 'ac_add_options --disable-debug' >> .mozconfig
          echo 'ac_add_options --disable-tests' >> .mozconfig
          echo 'ac_add_options --disable-crashreporter' >> .mozconfig

          ./mach build

      - name: Package
        run: |
          cd "$ROOT"
          ./mach package

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: ${{ env.ROOT }}/dist/


  windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      # ✅ Correct Python (fixes your error)
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ✅ Install MozillaBuild (REQUIRED)
      - name: Install MozillaBuild
        run: |
          curl -L -o mozillabuild.exe https://ftp.mozilla.org/pub/mozilla/libraries/win32/MozillaBuildSetup-Latest.exe
          Start-Process -Wait -FilePath .\mozillabuild.exe -ArgumentList "/S"
        shell: powershell

      - name: Set env
        run: |
          echo "MOZILLABUILD=C:\mozilla-build" >> $env:GITHUB_ENV
          echo "PATH=C:\mozilla-build\bin;$env:PATH" >> $env:GITHUB_ENV
        shell: powershell

      - name: Download source
        run: |
          curl -L -o source.tar.gz https://codeberg.org/api/packages/librewolf/generic/librewolf-source/149.0.2-2/librewolf-149.0.2-2.source.tar.gz

      - name: Extract
        run: tar xf source.tar.gz

      - name: Detect root
        run: |
          ROOT=$(find . -type f -path "*/dom/base/Navigator.cpp" | head -n 1 | sed 's|/dom/base/Navigator.cpp||')
          echo "ROOT=$ROOT" >> $GITHUB_ENV
        shell: bash

      - name: Apply patch
        run: |
          cd "$ROOT"
          patch -p1 < ../webdriver-disable.patch
        shell: bash

      - name: Build (low memory)
        run: |
          cd "$ROOT"

          export MOZ_PARALLEL_BUILD=2
          export MOZ_MAKE_FLAGS="-j2"

          echo 'ac_add_options --disable-debug' >> .mozconfig
          echo 'ac_add_options --disable-tests' >> .mozconfig

          ./mach build
        shell: bash

      - name: Package
        run: |
          cd "$ROOT"
          ./mach package
        shell: bash

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: ${{ env.ROOT }}/dist/


  release:
    needs: [linux, windows]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: linux-build
          path: linux

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: windows-build
          path: windows

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: ${{ github.event.inputs.version }}
          files: |
            linux/**
            windows/**
